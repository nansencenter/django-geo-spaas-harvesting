# Dependency requirements that can be used to build a working environment.
# For example, this is used to build the Docker image.

# strict requirements
pythesint==1.7.3.dev0

# loose requirements
copernicusmarine==2.*
django-geo-spaas==3.0.0.dev1
django==3.*
feedparser==6.*
feedparser==6.0.*
graypy==2.*
graypy==2.1.*
nansat==1.*
netCDF4==1.*
numpy==1.*
oauthlib==3.*
python-dateutil==2.*
PyYAML==5.*
requests_oauthlib==1.*
requests_oauthlib==1.3.*
requests==2.*
shapely==2.*

# necessary for running unit tests in parallel
tblib
